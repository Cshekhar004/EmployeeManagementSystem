@model IEnumerable<EmployeeManagement.Models.AuditLog>

@{
    ViewData["Title"] = "Audit Trail";
}

<div class="container mt-4 mb-4">

    <div class="card shadow">

        <div class="card-header bg-primary text-white p-3">

            <div class="d-flex justify-content-between align-items-center flex-wrap">

                <div>
                    <h4 class="mb-1">
                        <i class="bi bi-clock-history"></i>
                        Audit Trail
                    </h4>

                    <small class="opacity-75">
                        Track system activities, user actions, and security events
                    </small>
                </div>

                <span class="badge bg-light text-dark fs-6 px-3 py-2">
                    Total: @ViewBag.TotalRecords
                </span>

            </div>

        </div>

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-bordered table-hover align-middle">

                    <thead class="table-dark">
                        <tr>
                            <th>Module</th>
                            <th>Action</th>
                            <th>Record</th>
                            <th>Performed By</th>
                            <th>Date</th>
                        </tr>
                    </thead>

                    <tbody>

                        @if (Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>

                                    <td>
                                        <strong>
                                            @item.Module
                                        </strong>    
                                    </td>

                                    <td>
                                        @{
                                            string actionClass =
                                                item.Action == "Created" ? "bg-success" :
                                                item.Action == "Updated" ? "bg-warning text-dark" :
                                                item.Action == "Deleted" ? "bg-danger" :
                                                item.Action == "Activated" ? "bg-success" :
                                                item.Action == "Deactivated" ? "bg-danger" :
                                                item.Action == "Password Reset" ? "bg-secondary" :
                                                "bg-info";
                                        }

                                        <span class="badge @actionClass">
                                            @item.Action
                                        </span>
                                    </td>

                                    <td>
                                        <strong>
                                            @item.RecordInfo
                                        </strong>
                                    </td>

                                    <td>
                                        <i class="bi bi-person-circle text-primary"></i>
                                        @item.Username
                                    </td>

                                    <td class="text-nowrap">
                                        <i class="bi bi-calendar-event text-muted"></i>
                                        @item.ActionDate.ToString("dd-MMM-yyyy hh:mm tt")
                                    </td>

                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5"
                                    class="text-center text-muted py-4">

                                    No audit records found.

                                </td>
                            </tr>
                        }

                    </tbody>

                </table>

            </div>

            <nav>

                <ul class="pagination justify-content-center">

                    @if (ViewBag.CurrentPage > 1)
                    {
                        <li class="page-item">

                            <a class="page-link"
                               asp-action="Index"
                               asp-route-page="@(ViewBag.CurrentPage - 1)">
                                Previous
                            </a>

                        </li>
                    }

                    @for (int i = 1; i <= ViewBag.TotalPages; i++)
                    {
                        <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">

                            <a class="page-link"
                               asp-action="Index"
                               asp-route-page="@i">
                                @i
                            </a>

                        </li>
                    }

                    @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                    {
                        <li class="page-item">

                            <a class="page-link"
                               asp-action="Index"
                               asp-route-page="@(ViewBag.CurrentPage + 1)">
                                Next
                            </a>

                        </li>
                    }

                </ul>

            </nav>

        </div>

    </div>

</div>